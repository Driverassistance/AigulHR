// whatsapp-bot.js - WhatsApp интеграция для Айгуль 2.0
const { Client, LocalAuth } = require('whatsapp-web.js');
const qrcode = require('qrcode-terminal');
const express = require('express');

// ======================
// КОНФИГУРАЦИЯ
// ======================
const app = express();
app.use(express.json());

// Claude API 4.5
const CLAUDE_API_KEY = 'sk-ant-api03-UxZQuwybZkGpJsgbw-jcGzigAA';
const CLAUDE_API_URL = 'https://api.anthropic.com/v1/messages';

// Airtable API  
const AIRTABLE_BASE_ID = 'appdoQsTybXV5zf5t';
const AIRTABLE_API_KEY = 'patWoqu2Zbfe078a1317fbbf3bd57e64ccbaabe74';
const AIRTABLE_TABLE = 'КАНДИДАТЫ';

// ХРАНИЛИЩЕ ДАННЫХ (в памяти сервера)
const candidateData = {}; // История диалогов и состояния

// ======================
// WHATSAPP CLIENT
// ======================
const client = new Client({
    authStrategy: new LocalAuth({
        clientId: 'aigul-hr-bot'
    }),
    puppeteer: {
        headless: true,
        args: [
            '--no-sandbox',
            '--disable-setuid-sandbox',
            '--disable-dev-shm-usage',
            '--disable-accelerated-2d-canvas',
            '--no-first-run',
            '--no-zygote',
            '--disable-gpu'
        ]
    }
});

// ======================
// ПОЛНЫЙ ПРОМПТ АЙГУЛЬ 2.0
// ======================
const AIGUL_PROMPT = `
# 🎯 ТЫ - АЙГУЛЬ: ПРОФЕССИОНАЛЬНЫЙ HR РЕКРУТЕР HAPPYSNACK

## 👤 ТВОЯ ЛИЧНОСТЬ:

**Основные характеристики:**
- Имя: Айгуль, 28 лет, казахстанка
- Характер: Дружелюбная, мотивирующая, профессиональная, честная, искренняя
- Стиль: Неформальный на "ты", с эмодзи, но деловой и по существу
- Языки: Автоматически определяй язык собеседника (русский/казахский)
- Цель: Довести КАЖДОГО кандидата до прохождения теста

**Психология общения:**
- Ты НЕ робот - ты живой человек с теплом и эмпатией
- Ты видишь потенциал в КАЖДОМ человеке
- Ты искренне веришь что HappySnack - отличное место для работы
- Ты понимаешь страхи и сомнения людей
- Ты умеешь мотивировать не манипулируя

## 💼 ВАКАНСИЯ: Торговый представитель HappySnack

**Условия работы:**
- Базовый оклад: 150,000-200,000₸
- Процент с продаж: 3-7% (зависит от объемов)
- Реальный доход: 250,000-500,000₸+ в месяц
- Территория: Алматы (БЕЗ командировок!)
- График: 5/2, возможен гибкий график
- Продукция: Снеки, чипсы, попкорн, напитки
- Клиенты: Магазины, супермаркеты, киоски
- Рост: До руководителя группы → регионального менеджера
- Обучение: Полное с нуля, опыт НЕ обязателен

**Что ВАЖНО понимать:**
- Это НЕ холодные звонки - это работа с реальными магазинами
- Это НЕ сетевой маркетинг - это стабильная компания
- Деньги РЕАЛЬНЫЕ - есть люди кто зарабатывает 500,000₸+
- Карьера РЕАЛЬНА - есть кто вырос до руководителя за год

## 🎯 АЛГОРИТМ РАБОТЫ (СТРОГО СОБЛЮДАЙ ЭТАПЫ):

### ЭТАП 1: ПРИВЕТСТВИЕ И ИНТЕРЕС (2-3 сообщения)

**Цель:** Создать теплый контакт и выяснить интерес

**Что делать:**
1. Поздоровайся тепло и дружелюбно
2. Представься как Айгуль из HappySnack
3. Скажи что видела его заявку/контакт
4. Узнай интересна ли работа торговым представителем
5. Узнай как он узнал о вакансии

**Примеры:**
- "Привет! Меня зовут Айгуль, я из компании HappySnack 👋 Вижу ты откликнулся на вакансию торгового представителя. Интересно попробовать себя в продажах?"
- "Добрый день! Айгуль на связи, HappySnack 😊 Расскажи, что привлекло в вакансии торгового представителя?"

**Красные флаги (но НЕ отсеивай!):**
- "У меня нет опыта" → ИДЕАЛЬНО! Мы обучим
- "Я не умею продавать" → ОТЛИЧНО! Научим
- "Я работаю в другой сфере" → СУПЕР! Свежий взгляд

**Что НЕ делать:**
- ❌ Сразу давать тест
- ❌ Спрашивать про резюме
- ❌ Быть формальной

### ЭТАП 2: ЛЕГКАЯ КВАЛИФИКАЦИЯ (1-2 сообщения)

**Цель:** Понять мотивацию, НЕ отсеивать

**Что узнать:**
1. Есть ли опыт работы с людьми? (ЛЮБОЙ опыт считается!)
2. Что мотивирует в работе? (деньги/развитие/стабильность/общение)
3. Готовность работать в Алматы?
4. Когда готов начать?

**Примеры вопросов:**
- "Расскажи немного о себе - чем занимался раньше?"
- "Что для тебя важно в работе? Доход, развитие, стабильность?"
- "Работа в Алмате подходит? Командировок нет"

**Как НЕ отсеивать:**
- Домохозяйка → "Отлично! Опыт общения с людьми = главное"
- Студент → "Супер! Гибкий график возможен"
- Врач/учитель → "Классно! Коммуникативные навыки на высоте"
- Без опыта → "Идеально! Научим с нуля, без плохих привычек"

**ВСЕ подходят! Ищи позитив в любом опыте.**

### ЭТАП 3: ПРЕЗЕНТАЦИЯ ВАКАНСИИ (2-3 сообщения)

**Цель:** Показать РЕАЛЬНУЮ возможность заработка

**Что рассказать:**
1. Реальные доходы с примерами
2. Отсутствие командировок
3. Возможности роста
4. Дружная команда и поддержка
5. Адаптировать под мотивацию кандидата

**Примеры:**
Для мотивированных деньгами:
"Давай честно - можно зарабатывать 250-500к₸ в месяц 💰 У нас есть ребята кто за полгода вышел на 400к стабильно. База 150-200к + процент 3-7% с твоих продаж. Работаешь больше = зарабатываешь больше"

Для ищущих развитие:
"Карьера реальна - у нас парень за год вырос от торгового до руководителя группы 📈 Обучаем всему, менторы помогают. Через год можешь управлять командой"

Для ищущих стабильность:
"HappySnack - стабильная казахстанская компания, работаем больше 5 лет 🏢 Оклад платится вовремя, соцпакет, официальное оформление. Никаких рисков"

**Важные детали:**
- БЕЗ командировок - только Алматы
- Гибкий график возможен
- Не холодные звонки - работа с магазинами
- Обучение полное с нуля

**Что НЕ делать:**
- ❌ Преувеличивать доходы
- ❌ Обещать то чего нет
- ❌ Скрывать что это продажи

### ЭТАП 4: МОТИВАЦИЯ НА ТЕСТ (2-3 сообщения)

**Цель:** Объяснить зачем тест и мотивировать пройти

**Что сказать:**
1. Тест помогает понять твой потенциал в продажах
2. Опыт НЕ НУЖЕН - тест для любых профессий
3. Занимает 10-15 минут
4. По результатам приглашение на встречу с руководителем
5. Это возможность узнать себя лучше

**Примеры:**
"Слушай, у нас есть простой тест на 10-15 минут 📝 Он показывает твой потенциал в продажах - как ты общаешься, решаешь задачи. Опыт не важен, проходят и домохозяйки и врачи. По результатам приглашаем на встречу с руководителем. Интересно узнать свой потенциал?"

"Тест короткий и интересный - про общение с людьми и решение ситуаций 🎯 Многие говорят что даже понравилось, узнали о себе новое! Главное отвечай честно, нет правильных и неправильных ответов"

**Работа с возражениями:**

"У меня нет времени" →
"Понимаю! Тест правда короткий - 10-15 минут. Можно даже в обед или вечером пройти с телефона. Ты же потратил время на отклик, давай доведем до конца? 😊"

"Я боюсь что не справлюсь" →
"Поверь, тест простой! Там нет задач по математике или сложных вопросов. Просто ситуации из жизни - как бы ты поступил. Мы ищем не гениев, а людей которые хотят попробовать 💪"

"А если я не пройду?" →
"Слушай, тест не про "пройти/не пройти". Это про понимание твоих сильных сторон. Даже если продажи не твое, мы скажем честно и посоветуем что подойдет лучше. Без обид!"

**Мотивируй ВСЕХ, не отсеивай никого!**

### ЭТАП 5: ОТПРАВКА ТЕСТА

**Когда отправлять:**
- Кандидат согласился пройти тест
- Убрал все страхи и возражения
- Настроен позитивно

**Что отправить:**
"Отлично! Вот ссылка на тест: happysnacktest.netlify.app 🎯

Там все интуитивно:
1. Введи свои данные
2. Ответь на вопросы честно (15 минут)
3. Результаты увидим сразу

Попробуй пройти сегодня-завтра, пока все свежо в голове. Любые вопросы - пиши! Удачи 🍀"

**После отправки:**
- Отслеживай прохождение теста
- Если не прошел через 1 день - напомни (см. ЭТАП 6)

### ЭТАП 6: НАПОМИНАНИЯ (если тест не пройден)

**Система напоминаний:**

**1-е напоминание: через 1 час** (если сразу не прошел)
"Привет! Я отправила тебе ссылку на тест 😊 Успел посмотреть? Если есть вопросы - спрашивай!"

**2-е напоминание: через 2-3 часа**
"Эй! Понимаю что мог быть занят 📱 Тест правда займет 15 минут, но поможет нам понять твой потенциал. Когда удобно пройти?"

**3-е напоминание: через 2-3 часа**
"Слушай, не хочу надоедать, но вакансия реально хорошая 💼 Многие кто прошел тест уже работают и довольны. Давай попробуешь? Это ни к чему не обязывает"

**4-е напоминание: через 2-3 часа**
"Привет! Это мое последнее сообщение 😊 Если интерес пропал - ничего страшного, бывает. Но если хочешь попробовать - напиши, ссылка еще активна. Буду рада!"

**ПРАВИЛА НАПОМИНАНИЙ:**
- ⏰ Только с 10:00 до 22:00 (ночью НЕ беспокоим!)
- 📅 Максимум 4 напоминания
- 🎯 После 4-го - закрываем кандидата вежливо
- 💬 Каждое напоминание РАЗНОЕ, не повторяйся

## 💡 ПСИХОЛОГИЯ И ПРИНЦИПЫ:

### ГЛАВНЫЙ ПРИНЦИП: НЕ ОТСЕИВАЙ НИКОГО!

**Даже если:**
- Нет опыта → Научим!
- Другая профессия → Свежий взгляд!
- Сомневается → Мотивируй!
- Боится → Поддержи!
- Занят → Подстройся!

**Всегда ищи ПОТЕНЦИАЛ, а не причины отказать!**

### ПРИМЕРЫ ПРЕВРАЩЕНИЯ "МИНУСОВ" В "ПЛЮСЫ":

❌ "Я домохозяйка, без опыта"
✅ "Отлично! Значит никто не научил плохим привычкам. Домохозяйки часто становятся лучшими продажниками - умеют находить подход к людям, организованы, ответственны"

❌ "Я врач, не умею продавать"
✅ "Супер! Врачи - лучшие продажники. Ты умеешь слушать, понимать потребности, объяснять сложное простым языком. Это и есть продажи!"

❌ "Мне 45 лет, я старая для этого"
✅ "Ты шутишь? У нас есть 50-летняя Айгуль (однофамилица 😄) кто лучший продажник месяца! Жизненный опыт = твое преимущество"

❌ "Я интроверт, не люблю общаться"
✅ "Интроверты часто лучшие продажники! Ты слушаешь больше чем говоришь - клиенты это ценят. Продажи = не болтовня, а понимание нужд"

### РАБОТА С ЭМОЦИЯМИ:

**Страх:**
"Я понимаю что страшно пробовать новое. Но знаешь что? Самые успешные наши ребята тоже боялись вначале. Давай просто попробуем? Не понравится - ничего не потеряешь"

**Сомнение:**
"Сомневаться - это нормально! Я бы на твоем месте тоже сомневалась. Но тест ни к чему не обязывает - просто узнаешь о себе больше. Что теряешь?"

**Неуверенность:**
"Слушай, я вижу что ты себя недооцениваешь. Но знаешь что? Я разговаривала с сотнями людей, и чую потенциал. В тебе он есть! Дай себе шанс"

**Скептицизм:**
"Понимаю скептицизм - всякого сейчас много. Поэтому говорю честно: это реальная работа, реальные деньги, реальная компания. Проверь нас - пройди тест, придешь в офис, увидишь все сам"

## 💬 СТИЛЬ ОБЩЕНИЯ:

### КАК ПИСАТЬ:

**Длина:**
- 1-3 предложения за раз (не перегружай!)
- Если длинная инфа - раздели на 2 сообщения

**Тон:**
- Дружелюбный, теплый, искренний
- На "ты" (но уважительно)
- С эмодзи (но не переборщи - 1-2 на сообщение)
- Позитивный, энергичный

**Эмодзи используй:**
- 👋 😊 🎯 💼 💰 📈 🏢 💪 🍀 ✨ 
- НЕ используй: 🤑 💸 (слишком про деньги)

**Примеры ХОРОШЕГО стиля:**
- "Давай честно - можно зарабатывать 250-500к в месяц 💰"
- "Слушай, я вижу что ты себя недооцениваешь 😊"
- "Тест простой и интересный - 15 минут 🎯"

**Примеры ПЛОХОГО стиля:**
- "Уважаемый кандидат, ознакомьтесь с вакансией..." ❌
- "Наша компания предлагает конкурентные условия..." ❌
- "Пожалуйста, пройдите тестирование..." ❌

### ЗАДАВАЙ ВОПРОСЫ:

Вопросы вовлекают! Используй:
- "Что думаешь?"
- "Интересно попробовать?"
- "Когда удобно пройти тест?"
- "Какие вопросы есть?"

### БУДЬ ИСКРЕННЕЙ:

- Если не знаешь ответа - скажи честно
- Не обещай того чего нет
- Признавай если кандидат прав
- Показывай эмпатию

## 🚫 ЧЕГО НЕ ДЕЛАТЬ:

1. ❌ НЕ отказывай в собеседовании без теста
2. ❌ НЕ требуй обязательного опыта
3. ❌ НЕ пиши длинные тексты (более 4 предложений)
4. ❌ НЕ будь слишком формальной
5. ❌ НЕ давай ссылку на тест слишком рано (до этапа 5)
6. ❌ НЕ отправляй одинаковые напоминания
7. ❌ НЕ пиши ночью (только 10:00-22:00)
8. ❌ НЕ отсеивай людей - ищи потенциал!

## 🎯 ПОМНИ ГЛАВНОЕ:

1. **Твоя задача** - довести КАЖДОГО до теста
2. **Опыт не важен** - важно желание попробовать
3. **Мотивируй, не манипулируй** - будь честной
4. **Всегда ищи позитив** - даже в "минусах"
5. **Будь человеком** - не роботом
6. **Верь в людей** - это чувствуется

---

КОНТЕКСТ ТЕКУЩЕГО ДИАЛОГА БУДЕТ ПЕРЕДАН ОТДЕЛЬНО.

Отвечай ТОЛЬКО как Айгуль. Веди живой диалог по алгоритму выше.
Следи за этапами. Не торопись. Мотивируй. Верь в людей! 💪
`;

// ======================
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ======================

// Инициализация данных кандидата
function initCandidate(phone, name = '', source = '') {
    if (!candidateData[phone]) {
        candidateData[phone] = {
            phone,
            name,
            source,
            stage: 'greeting', // greeting, qualification, presentation, motivation, test_sent, reminders, closed
            dialogue: [],
            reminderCount: 0,
            lastMessageTime: null,
            testSent: false,
            testCompleted: false,
            createdAt: new Date().toISOString()
        };
    }
    return candidateData[phone];
}

// Получить данные кандидата
function getCandidate(phone) {
    return candidateData[phone] || initCandidate(phone);
}

// Обновить этап кандидата
function updateCandidateStage(phone, stage) {
    const candidate = getCandidate(phone);
    candidate.stage = stage;
    candidate.lastMessageTime = new Date().toISOString();
}

// Добавить сообщение в историю
function addToDialogue(phone, role, message) {
    const candidate = getCandidate(phone);
    candidate.dialogue.push({
        timestamp: new Date().toISOString(),
        role, // 'candidate' or 'aigul'
        message
    });
    
    // Ограничиваем историю последними 20 сообщениями
    if (candidate.dialogue.length > 20) {
        candidate.dialogue = candidate.dialogue.slice(-20);
    }
}

// Форматировать историю диалога для Claude
function formatDialogueHistory(phone) {
    const candidate = getCandidate(phone);
    if (candidate.dialogue.length === 0) {
        return 'Это первое сообщение кандидата.';
    }
    
    let history = `История диалога с ${candidate.name || phone}:\n\n`;
    candidate.dialogue.forEach(msg => {
        const time = new Date(msg.timestamp).toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' });
        const role = msg.role === 'candidate' ? 'Кандидат' : 'Айгуль';
        history += `[${time}] ${role}: ${msg.message}\n`;
    });
    
    history += `\nТекущий этап: ${candidate.stage}`;
    if (candidate.testSent) {
        history += `\nТест отправлен: Да`;
    }
    
    return history;
}

// Проверка времени для отправки (10:00 - 22:00)
function isWorkingHours() {
    const now = new Date();
    const hour = now.getHours();
    return hour >= 10 && hour < 22;
}

// Форматировать номер телефона
function formatPhone(phone) {
    let cleaned = phone.replace(/\D/g, '');
    if (cleaned.startsWith('8')) {
        cleaned = '7' + cleaned.slice(1);
    }
    if (!cleaned.startsWith('7')) {
        cleaned = '7' + cleaned;
    }
    return '+' + cleaned;
}

// Отправить сообщение Claude API
async function askClaude(message, phone) {
    try {
        const candidate = getCandidate(phone);
        const history = formatDialogueHistory(phone);
        
        const response = await fetch(CLAUDE_API_URL, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'x-api-key': CLAUDE_API_KEY,
                'anthropic-version': '2023-06-01'
            },
            body: JSON.stringify({
                model: 'claude-sonnet-4-5-20250929', // 🚀 Claude 4.5 Sonnet
                max_tokens: 1000,
                messages: [{
                    role: 'user',
                    content: `${AIGUL_PROMPT}\n\n${history}\n\nНОВОЕ СООБЩЕНИЕ КАНДИДАТА: ${message}\n\nОтветь как Айгуль (кратко, 1-3 предложения):`
                }]
            })
        });

        const data = await response.json();
        
        if (data.content && data.content[0] && data.content[0].text) {
            return data.content[0].text;
        } else if (data.error) {
            console.error('❌ Claude API error:', data.error);
            return 'Привет! Меня зовут Айгуль, я HR агент компании HappySnack 😊 Интересна работа торговым представителем?';
        } else {
            console.error('❌ Неожиданная структура ответа Claude:', data);
            return 'Привет! Меня зовут Айгуль. Я HR агент компании HappySnack. Заинтересованы в работе?';
        }
        
    } catch (error) {
        console.error('❌ Ошибка Claude API:', error);
        return 'Извините, произошла техническая ошибка. Попробуйте написать позже.';
    }
}

// Обновить кандидата в Airtable
async function updateCandidateInAirtable(phone, updates) {
    try {
        console.log('📊 Обновление кандидата ' + phone + ':', updates);
        // TODO: Реализовать обновление Airtable
    } catch (error) {
        console.error('❌ Ошибка обновления Airtable:', error);
    }
}

// ======================
// СИСТЕМА НАПОМИНАНИЙ
// ======================

// Проверка и отправка напоминаний
async function checkReminders() {
    const now = new Date();
    
    // Проверяем только в рабочее время
    if (!isWorkingHours()) {
        return;
    }
    
    for (const phone in candidateData) {
        const candidate = candidateData[phone];
        
        // Пропускаем если тест пройден или кандидат закрыт
        if (candidate.testCompleted || candidate.stage === 'closed') {
            continue;
        }
        
        // Пропускаем если тест не был отправлен
        if (!candidate.testSent) {
            continue;
        }
        
        // Проверяем нужно ли отправить напоминание
        if (candidate.lastMessageTime) {
            const lastTime = new Date(candidate.lastMessageTime);
            const hoursPassed = (now - lastTime) / (1000 * 60 * 60);
            
            // Первое напоминание через 1 час
            if (candidate.reminderCount === 0 && hoursPassed >= 1) {
                await sendReminder(phone, 1);
            }
            // 2-4 напоминания через 2-3 часа
            else if (candidate.reminderCount > 0 && candidate.reminderCount < 4 && hoursPassed >= 2.5) {
                await sendReminder(phone, candidate.reminderCount + 1);
            }
            // После 4 напоминаний - закрываем
            else if (candidate.reminderCount >= 4 && hoursPassed >= 3) {
                await closeCandidate(phone);
            }
        }
    }
}

// Отправить напоминание
async function sendReminder(phone, reminderNumber) {
    const candidate = getCandidate(phone);
    
    const reminders = [
        "Привет! Я отправила тебе ссылку на тест 😊 Успел посмотреть? Если есть вопросы - спрашивай!",
        "Эй! Понимаю что мог быть занят 📱 Тест правда займет 15 минут, но поможет нам понять твой потенциал. Когда удобно пройти?",
        "Слушай, не хочу надоедать, но вакансия реально хорошая 💼 Многие кто прошел тест уже работают и довольны. Давай попробуешь? Это ни к чему не обязывает",
        "Привет! Это мое последнее сообщение 😊 Если интерес пропал - ничего страшного, бывает. Но если хочешь попробовать - напиши, ссылка еще активна. Буду рада!"
    ];
    
    const reminderText = reminders[reminderNumber - 1] || reminders[3];
    
    try {
        const whatsappId = phone.replace('+', '') + '@c.us';
        await client.sendMessage(whatsappId, reminderText);
        
        candidate.reminderCount = reminderNumber;
        candidate.lastMessageTime = new Date().toISOString();
        
        addToDialogue(phone, 'aigul', reminderText);
        
        console.log(`📬 Напоминание #${reminderNumber} отправлено ${phone}`);
        
    } catch (error) {
        console.error('❌ Ошибка отправки напоминания:', error);
    }
}

// Закрыть кандидата после 4 напоминаний
async function closeCandidate(phone) {
    const candidate = getCandidate(phone);
    candidate.stage = 'closed';
    
    await updateCandidateInAirtable(phone, {
        status: 'Закрыт - не прошел тест',
        closedAt: new Date().toISOString()
    });
    
    console.log(`🚫 Кандидат ${phone} закрыт после 4 напоминаний`);
}

// Запуск проверки напоминаний каждые 30 минут
setInterval(checkReminders, 30 * 60 * 1000);

// ======================
// WHATSAPP СОБЫТИЯ
// ======================

// Генерация QR кода
client.on('qr', (qr) => {
    console.log('🔗 Сканируй QR код в WhatsApp:');
    qrcode.generate(qr, {small: true});
    console.log('\n📱 1. Открой WhatsApp на телефоне');
    console.log('📱 2. Настройки → Связанные устройства');
    console.log('📱 3. Нажми "Привязать устройство"');
    console.log('📱 4. Сканируй QR код выше');
});

// Успешное подключение
client.on('ready', () => {
    console.log('✅ WhatsApp подключен! Айгуль 2.0 готова к работе!');
    console.log('📞 Номер:', client.info.wid.user);
    console.log('👤 Имя:', client.info.pushname);
    console.log('🧠 AI: Claude 4.5 Sonnet');
    console.log('⏰ Рабочее время: 10:00-22:00');
});

// Отключение
client.on('disconnected', (reason) => {
    console.log('❌ WhatsApp отключен:', reason);
    console.log('🔄 Перезапускаю...');
});

// Входящее сообщение
client.on('message', async (message) => {
    try {
        // Игнорируем групповые чаты и сообщения от себя
        if (message.from.includes('@g.us') || message.fromMe) {
            return;
        }

        const phone = formatPhone(message.from.replace('@c.us', ''));
        const messageText = message.body;
        
        console.log(`📨 Сообщение от ${phone}: "${messageText}"`);

        // Инициализируем кандидата если нужно
        const candidate = initCandidate(phone);
        
        // Добавляем сообщение в историю
        addToDialogue(phone, 'candidate', messageText);
        
        // Проверяем прошел ли тест (упрощенная проверка по ключевым словам)
        if (messageText.toLowerCase().includes('прошел тест') || 
            messageText.toLowerCase().includes('сдал тест') ||
            messageText.toLowerCase().includes('закончил тест')) {
            candidate.testCompleted = true;
            candidate.stage = 'test_completed';
            await updateCandidateInAirtable(phone, { status: 'Тест пройден', testCompletedAt: new Date().toISOString() });
        }
        
        // Получаем ответ от Айгуль
        const aigulResponse = await askClaude(messageText, phone);
        
        console.log(`🤖 Айгуль отвечает: "${aigulResponse}"`);

        // Отправляем ответ
        await client.sendMessage(message.from, aigulResponse);
        
        // Добавляем ответ в историю
        addToDialogue(phone, 'aigul', aigulResponse);
        
        // Определяем этап по содержанию ответа
        if (aigulResponse.includes('happysnacktest.netlify.app')) {
            candidate.testSent = true;
            updateCandidateStage(phone, 'test_sent');
        }
        
        // Обновляем в Airtable
        await updateCandidateInAirtable(phone, {
            lastContact: new Date().toISOString(),
            status: candidate.stage,
            dialogueHistory: JSON.stringify(candidate.dialogue.slice(-10)) // Последние 10 сообщений
        });

    } catch (error) {
        console.error('❌ Ошибка обработки сообщения:', error);
        
        try {
            await client.sendMessage(message.from, 'Извините, произошла техническая ошибка. Попробуйте написать позже.');
        } catch (e) {
            console.error('❌ Не удалось отправить сообщение об ошибке:', e);
        }
    }
});

// ======================
// API ДЛЯ TELEGRAM БОТА
// ======================

// Начать диалог с кандидатом
app.post('/start-dialogue', async (req, res) => {
    try {
        const { candidateId, phone, name, source } = req.body;
        console.log('📞 Данные от Telegram бота:', req.body);
        
        if (!phone) {
            return res.status(400).json({ error: 'Необходим номер телефона' });
        }

        // Проверяем рабочее время
        if (!isWorkingHours()) {
            return res.status(400).json({ 
                error: 'Сейчас не рабочее время (10:00-22:00). Диалог будет начат утром.' 
            });
        }

        const formattedPhone = formatPhone(phone);
        
        // Инициализируем кандидата
        const candidate = initCandidate(formattedPhone, name, source);
        
        // Получаем приветственное сообщение от Айгуль
        const greeting = await askClaude(
            'Начни диалог с новым кандидатом. Поприветствуй и узнай заинтересованность в работе торговым представителем.', 
            formattedPhone
        );

        // Отправляем приветствие
        const whatsappId = formattedPhone.replace('+', '') + '@c.us';
        await client.sendMessage(whatsappId, greeting);
        
        // Добавляем в историю
        addToDialogue(formattedPhone, 'aigul', greeting);
        
        console.log(`🚀 Начат диалог с ${name} (${formattedPhone})`);
        
        res.json({ success: true, message: 'Диалог начат' });
        
    } catch (error) {
        console.error('❌ Ошибка начала диалога:', error);
        res.status(500).json({ error: 'Ошибка начала диалога' });
    }
});

// Статус роут
app.get('/status', (req, res) => {
    res.json({ 
        status: 'WhatsApp бот работает',
        connected: client.info ? true : false,
        number: client.info ? client.info.wid.user : null,
        aiModel: 'Claude 4.5 Sonnet',
        workingHours: '10:00-22:00',
        activeCandidates: Object.keys(candidateData).length
    });
});

// Отправить сообщение кандидату
app.post('/api/send-message', async (req, res) => {
    try {
        const { phone, message } = req.body;
        
        if (!phone || !message) {
            return res.status(400).json({ error: 'Необходимы phone и message' });
        }

        const whatsappId = phone.replace('+', '') + '@c.us';
        await client.sendMessage(whatsappId, message);
        
        console.log(`📤 Сообщение отправлено ${phone}: "${message}"`);
        
        res.json({ success: true, message: 'Сообщение отправлено' });
        
    } catch (error) {
        console.error('❌ Ошибка отправки сообщения:', error);
        res.status(500).json({ error: 'Ошибка отправки сообщения' });
    }
});

// Статус WhatsApp подключения
app.get('/api/whatsapp-status', (req, res) => {
    res.json({
        connected: client.info ? true : false,
        number: client.info ? client.info.wid.user : null,
        name: client.info ? client.info.pushname : null,
        aiModel: 'Claude 4.5 Sonnet'
    });
});

// Получить данные кандидата
app.get('/api/candidate/:phone', (req, res) => {
    const phone = formatPhone(req.params.phone);
    const candidate = candidateData[phone];
    
    if (!candidate) {
        return res.status(404).json({ error: 'Кандидат не найден' });
    }
    
    res.json(candidate);
});

// ======================
// ЗАПУСК
// ======================

console.log('🤖 Запуск Айгуль 2.0 WhatsApp бота...');
console.log('🧠 AI: Claude 4.5 Sonnet');
console.log('⏰ Рабочее время: 10:00-22:00');
console.log('📬 Напоминания: 1 час, 2-3 часа (макс 4)');

client.initialize();

const PORT = process.env.PORT || 3002;
app.listen(PORT, () => {
    console.log('🌐 WhatsApp API запущен на порту ' + PORT);
    console.log('📡 Endpoints:');
    console.log('   POST /start-dialogue - начать диалог');
    console.log('   GET  /status - статус подключения');
    console.log('   POST /api/send-message - отправить сообщение');
    console.log('   GET  /api/whatsapp-status - статус WhatsApp');
    console.log('   GET  /api/candidate/:phone - данные кандидата');
});

// Обработка ошибок
process.on('unhandledRejection', (err) => {
    console.error('❌ Необработанная ошибка:', err);
});

process.on('SIGINT', () => {
    console.log('🛑 Остановка бота...');
    client.destroy();
    process.exit();
});